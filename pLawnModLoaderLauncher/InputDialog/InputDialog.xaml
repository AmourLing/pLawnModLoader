<Window
    x:Class="pLawnModLoaderLauncher.InputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="InputDialog"
    Width="300"
    Height="150"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="lblQuestion"
            Grid.Row="0"
            Margin="0,0,0,10" />
        <TextBox
            x:Name="txtAnswer"
            Grid.Row="1"
            Height="23"
            Margin="0,0,0,10" />
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                MinWidth="60"
                Margin="0,0,10,0"
                Click="btnDialogOk_Click"
                Content="确定"
                IsDefault="True" />
            <Button
                MinWidth="60"
                Content="取消"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>